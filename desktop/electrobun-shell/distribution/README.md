# Desktop Distribution Scaffolding

Placeholders for the eventual signed desktop beta distribution path.

Current intent:

- keep the release-channel/update/signing metadata location explicit
- avoid hiding external credential dependencies in one-off local scripts

Expected future pieces:

- signed artifact naming convention
- beta/stable channel metadata
- update feed URL
- signing/notarization instructions

Current repo placeholders:

- `channels.example.json`
- `macos-signing-checklist.md`
