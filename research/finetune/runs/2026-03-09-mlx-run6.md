# 2026-03-09 MLX Run 6

Purpose:

- first real multilingual-heavy mix experiment

Config:

- base shape matched the winning `run1`
- dataset: `mlx-multilingual-boost`

Training result:

- best validation loss: `0.675` at iter `300`
- final validation loss: `0.799`

Outcome:

- reject for promotion

Interpretation:

- boosting multilingual data too aggressively degraded the overall training signal
- multilingual coverage is important, but it should augment the core corpus rather than dominate it
