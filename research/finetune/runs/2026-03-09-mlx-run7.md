# 2026-03-09 MLX Run 7

Purpose:

- test the qmd-majority mix variant

Config:

- base shape matched `run1`
- dataset: `mlx-qmd-majority`

Observed:

- best validation loss before stop: `0.634` at iter `500`
- earlier best at `200`: `0.739`
- later regressions at `300`/`400`

Outcome:

- stopped early
- reject for promotion

Interpretation:

- restoring qmd majority alone is not enough
- broad mix changes are still too coarse
- next step should be a more targeted variant, not another large corpus swing
