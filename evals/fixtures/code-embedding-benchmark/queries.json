[
  {
    "id": "nl-jwt-middleware",
    "caseSet": "nl2code",
    "query": "middleware that validates a bearer token and attaches the authenticated user to the request",
    "relevantDocs": ["ts/jwt-middleware.ts"],
    "judgments": [{ "docid": "ts/jwt-middleware.ts", "relevance": 3 }]
  },
  {
    "id": "nl-connection-pool",
    "caseSet": "nl2code",
    "query": "reuse database connections instead of opening a new connection for every query",
    "relevantDocs": ["ts/connection-pool.ts"],
    "judgments": [{ "docid": "ts/connection-pool.ts", "relevance": 3 }]
  },
  {
    "id": "nl-debounce-search",
    "caseSet": "nl2code",
    "query": "debounce search input so network requests are not fired on every keystroke",
    "relevantDocs": ["ts/debounce-search.ts"],
    "judgments": [{ "docid": "ts/debounce-search.ts", "relevance": 3 }]
  },
  {
    "id": "nl-retry-queue",
    "caseSet": "nl2code",
    "query": "retry failed jobs with exponential backoff until max attempts is reached",
    "relevantDocs": ["python/retry_queue.py"],
    "judgments": [{ "docid": "python/retry_queue.py", "relevance": 3 }]
  },
  {
    "id": "nl-rate-limit",
    "caseSet": "nl2code",
    "query": "http middleware that rate limits requests per client ip using a token bucket",
    "relevantDocs": ["go/rate_limit.go"],
    "judgments": [{ "docid": "go/rate_limit.go", "relevance": 3 }]
  },
  {
    "id": "nl-lru-cache",
    "caseSet": "nl2code",
    "query": "least recently used cache with eviction when capacity is exceeded",
    "relevantDocs": ["rust/lru_cache.rs"],
    "judgments": [{ "docid": "rust/lru_cache.rs", "relevance": 3 }]
  },
  {
    "id": "identifier-tokenbucket",
    "caseSet": "identifier",
    "query": "tokenBucket",
    "relevantDocs": ["go/rate_limit.go"],
    "judgments": [{ "docid": "go/rate_limit.go", "relevance": 2 }]
  },
  {
    "id": "identifier-lrucache",
    "caseSet": "identifier",
    "query": "LruCache",
    "relevantDocs": ["rust/lru_cache.rs"],
    "judgments": [{ "docid": "rust/lru_cache.rs", "relevance": 2 }]
  }
]
