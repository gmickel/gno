---
satisfies: [R1, R2, R3]
---
# fn-149-request-local-retrieval-hydration-reuse.2 Share complete hydration through hybrid reranking and assembly

## Description
Share complete hydration through hybrid reranking and assembly. Implements the mapped requirements using the parent spec contract.

**Size:** M
**Files:** src/pipeline/hybrid.ts, src/pipeline/rerank.ts, src/pipeline/hydration.ts (new), test/pipeline/hydration.test.ts (new), test/pipeline/hybrid-doc-lookup.test.ts
**Touches:** [src/pipeline/hybrid.ts, src/pipeline/rerank.ts, src/pipeline/hydration.ts, test/pipeline/hydration.test.ts, test/pipeline/hybrid-doc-lookup.test.ts]

### Approach

- Pass one hydration owner through hybrid selection, rerank and assembly, reusing complete chunks already fetched instead of repeating store reads.
- Retain best-passage selection and existing4000-character preparation, dedup/index mapping and graph-added candidates; hydrate newly added candidates lazily within the same owner.
- Use fn-143 before/after model-input records as the equivalence harness. Keep fn-148 candidate-selection changes out of this task.

### Investigation targets

**Required:**
- `src/pipeline/rerank.ts:108`
- `src/pipeline/hybrid.ts:942`
- `test/pipeline/rerank-normalization.test.ts`
- `test/pipeline/hybrid-intent.test.ts`

### Execution constraints

- Preserve fn-143 fixture identities and use its paired comparator for mechanical equivalence. Scenario-specific additions have new hashes; do not refresh a baseline to hide a failure.
- Couple affected behavior/API documentation to the implementation change. Final QA reconciles prior docs; it does not postpone known contract updates.
- Shared source, migration and hosted-doc files require one editing owner at a time across specs; dependency independence is not permission for overlapping edits.
- Native probes use isolated synthetic state, cached models and one workload per GPU. Never restart the live service or benchmark the private vault.
- No formal plan-review or impl-review. Focused tests and captured running-surface QA remain required.

### Quick commands

```sh
bun test test/pipeline/hydration.test.ts test/pipeline/hybrid-doc-lookup.test.ts test/pipeline/rerank-normalization.test.ts test/pipeline/hybrid-intent.test.ts
```

New test/command paths listed above are deliverables; run them after creating them. Existing investigation paths were verified during planning.

## Acceptance
- [ ] Large duplicate-candidate fixture reduces repeated reads without any changed score/order/passage/model input.
- [ ] Intent/exclusion paths still receive complete required chunks; graph-added candidates retain prior behavior.
- [ ] Failure and abort release only request-owned caches.

## Done summary
# fn-149-request-local-retrieval-hydration-reuse.2

Shared one RequestHydration through hybrid graph candidate hydration, reranking, document lookup, full-content lookup and final assembly. Hybrid creates/releases its default owner in finally; a supplied internal owner remains the caller's responsibility for Ask integration. Reranking caches raw chunks only and retains existing passage selection, 4000-character preparation, deduplication, index mapping, blending and tie behavior. Graph expansion accepts an optional narrow hydration-read dependency (defaulting to its original store), so newly discovered hashes enter the same owner lazily without changing graph selection.

Changed files: src/pipeline/graph-retrieval.ts, src/pipeline/hybrid.ts, src/pipeline/rerank.ts, test/pipeline/hybrid-doc-lookup.test.ts, test/fixtures/pipeline/hybrid-hydration-baseline.json. Existing hydration helper and 1000-chunk fixture unchanged. Internal optimization, no public API/CLI contract or docs change.

Baseline: green, 41 tests. Final focused suite: 45 tests, 0 failures (329 assertions after final count assertion). Typed lint and formatting pass. Exact commands/logs in evidence JSON.

Frozen baseline generated by running the unmodified hybrid and rerank source at bdb2aae8c9c776a59a1e72072a26cfd7258be7cb before integration; baseline JSON records source-file SHA256 values and the unchanged input-fixture pin fd7fd4ba729385ffefcf59b423b9ca2b73891956f92cc97ac121e2fcae0d8c9e. Full JSON outputs and actual reranker-port arguments are compared using fn143 compareAcceptance, without pruning deterministic output fields. Plain, intent and exclusion requests include duplicate candidates from one 1000-chunk document and a graph-added neighbor. Baseline 9 reads / 6009 chunk rows / 13919514 UTF-8 text bytes; candidate 6 reads / 3003 rows / 6959733 bytes. These count actual fake-store returned rows/text, not total JS allocation or native costs. Existing generic hydration parity test also rejects deliberately shortened model input.

Completion, thrown reranker failure and abort tests retain active-stage snapshots and preserve caller ownership; abort releases the owner and rejects subsequent loads. Existing helper tests cover pending reads, fresh next requests, missing/error/throw, source mutations and document/collection isolation. All original graph, rerank-normalization, intent and exclusion regressions pass.

stage: impl-review - skipped(config: user disabled formal reviews)

Task remains in_progress. Host owns commits, full gates and physical paired CLI/API/native QA; no native or live-surface acceptance claimed. No Flow/Git lifecycle mutation performed.
## Evidence
- Commits: 7378a05f0ea43f9dff5ffd64162edd19bdc9bc82
- Tests: baseline: green, 41 tests, TMPDIR=/home/gordon/.cache/agent-tmp/gno-fn149-hybrid timeout 600 bun test ./test/pipeline/hydration.test.ts ./test/pipeline/hybrid-doc-lookup.test.ts ./test/pipeline/rerank-normalization.test.ts ./test/pipeline/hybrid-intent.test.ts, TMPDIR=/home/gordon/.cache/agent-tmp/gno-fn149-hybrid bunx oxlint --no-ignore --type-aware --type-check /home/gordon/work/gno/src/pipeline/hybrid.ts /home/gordon/work/gno/src/pipeline/graph-retrieval.ts /home/gordon/work/gno/src/pipeline/rerank.ts /home/gordon/work/gno/test/pipeline/hybrid-doc-lookup.test.ts, bunx oxfmt --check src/pipeline/hybrid.ts src/pipeline/graph-retrieval.ts src/pipeline/rerank.ts test/pipeline/hybrid-doc-lookup.test.ts test/fixtures/pipeline/hybrid-hydration-baseline.json
- PRs: