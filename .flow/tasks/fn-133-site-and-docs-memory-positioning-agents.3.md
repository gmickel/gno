---
satisfies: [R2, R5]
---
# fn-133-site-and-docs-memory-positioning-agents.3 Comparison + integration truth sweep

## Description
Comparison + integration truth sweep. **Size:** M. **Files/Touches (exclusive):** gno.sh src/lib/gno-comparisons.tsx, src/lib/integration-pages.ts, src/lib/public-truth-content.test.ts (the test currently LOCKS "25 read-only"/"15 write"/"40 tools" strings — update assertions to the profile story), plus tool-count strings in gno-docs.tsx integration/docs pages (coordinate: gno-docs memory/protocol pages belong to tasks 1/2; this task touches ONLY tool-count/capability lines elsewhere in that file).
qmd page: VERIFY against qmd's current README/changelog at execution time — do NOT hard-code this plan's claims (known drift as of 2026-09-01 review: structured_search renamed/removed; `generate` model does query expansion, not answer generation; "read-only" must be narrowed to its MCP/corpus-write surface). GBrain page memory section to shipped tense. Sweep ALL comparison + integration pages for tool-count and capability drift against the fn-131 profile story.

**Touches:** gno.sh: src/lib/gno-comparisons.tsx (exclusive), src/lib/integration-pages.ts (exclusive), src/lib/public-truth-content.test.ts, gno-docs.tsx tool-count lines only

## Acceptance
- [ ] qmd page claims re-verified against live qmd docs at execution (verification noted in the change); no stale claims remain
- [ ] public-truth-content.test.ts assertions updated to the profile story and green
- [ ] Tool-count sweep complete across comparisons + integrations + docs pages (list of touched claims in the PR)
- [ ] product-pages.ts untouched (task 1 owns it)

## Done summary
Comparison and integration truth sweep shipped in the gno.sh worktree (commit d0661d6b on fn-133-site-and-docs-memory-positioning-agents, base 20687f70): the qmd comparison page rewritten against qmd 2.8.3 as verified at execution time, the GBrain comparison memory section moved to shipped tense, every remaining 25/15/40 and 32/18/50 MCP tool count on comparison, integration, and docs pages moved to the v1.45.0 profile story (34 read-only, 19 write, 53 total; core profile 7 read + 2 write), and public-truth-content.test.ts extended so none of the earlier pairs can return. In the gno repo (commit e561ca64), docs/MCP.md's overview sentence and tool lists now match the registry.

qmd verification (do not trust this plan's earlier claims; these were fetched 2026-09-03): README on github.com/tobi/qmd main at commit dbfd0b4736aeaf761d1a16ca8e424f071df8feb9 (pushed 2026-08-18), CHANGELOG.md same commit, latest release v2.8.3 (2026-08-16; npm @tobilu/qmd 2.8.3 with node-llama-cpp 3.20.0 and @modelcontextprotocol/server 2.0.0). Findings applied: qmd is BM25 + vector + LLM reranking with a fine-tuned 1.7B query-expansion model behind the `generate` model role (no answer generation); `search` is BM25, `vsearch` vector, `query` hybrid with RRF fusion; the corpus is text files matched by a per-collection glob (`**/*.md` default) with AST chunking for TS/JS/Python/Go/Rust, no PDF or Office ingestion; the MCP server exposes four read tools (`query`, `get`, `multi_get`, `status`; `structured_search` was renamed to `query` in 1.1.0) over stdio or loopback HTTP with Origin/Host checks, and all corpus writes are CLI commands, so "read-only" on the page is now scoped to its MCP surface; 2.8.3 speaks MCP revision 2026-07-28 via SDK 2.x while serving 2025-era stdio clients. The page carries a dated verification line naming the commit and changelog date.

Touched claims (PR-facing list):
- gno.sh src/lib/gno-comparisons.tsx, qmd page: verdict, description, chooseThem, chooseGno, and the whole body (formats, retrieval, MCP surface with the 34/19/53 + core 7 (+2) profile line and both protocol revisions 2025-11-25 / 2026-07-28 from docs/MCP.md, memory split, beyond search, verification note). "Markdown only" and "BM25-style keyword search" removed as inaccurate for 2.8.3.
- gno.sh src/lib/gno-comparisons.tsx, GBrain page: description adds scoped remember/recall memory; positioning paragraph names gno remember / gno recall with the 8 facts / 512 tokens budget and gno:// cites; new "Memory" key-difference bullet (propose-first, add or supersede by URI + content hash, predecessor kept, recall receipt fence, four surfaces, Hermes provider reads and writes, OpenClaw plugin retrieves); "Running both" names gno_remember among the opt-in MCP writes. Sources: docs/MEMORY.md, src/core/memory-types.ts, integrations/hermes-gno-memory and openclaw-gno-memory READMEs at v1.45.0.
- gno.sh src/lib/integration-pages.ts: claude-desktop metaDescription (25 -> 34 read-only tools by default or a 7-tool core profile), claude-desktop unlock (15 mutating -> 19 write tools, gno_remember among them), amp metaDescription (25 -> 34), amp unlock (25 -> 34 read-only retrieval, memory recall, graph, and status tools by default, or a 7-tool core profile).
- gno.sh src/lib/gno-docs.tsx, tool-count lines only: MCP reference "What this is" read bullet (32 -> 34, adds scoped memory recall and egress checks, names the core profile's 7), write bullet (18/50 -> 19/53, adds remember facts and egress policy changes, core adds only gno_capture and gno_remember); cursor page (25 -> 34, Fifteen/40 -> Nineteen/53, adds memory recall); claude-desktop page (fifteen -> nineteen mutating tools, adds remember). No memory, agents-install, or protocol page region edited.
- gno.sh src/lib/public-truth-content.test.ts: STALE_TOOL_COUNTS constant (19/22/25/32/33 read-only, 11/15/18 write or mutating, 30/37/40/50/51 tools, "(40 total)", spelled fifteen/eighteen/twenty-five/thirty-two/thirty-three) replaces the inline negative regex in the verified-Ask test; new test "keeps MCP tool counts on the v1.45.0 profile story across comparisons, integrations, and docs" reads gno-docs, gno-comparisons, integration-pages, product-pages, site-content and pins the 34/19/53 lines, the qmd four-read-tools and "Checked against qmd 2.8.3" lines, and the absence of structured_search / "Markdown only". Red-first evidence: the regex matched the pre-edit gno-docs.tsx and integration-pages.ts (git show HEAD) and nothing after the sweep.
- gno docs/MCP.md overview: 33/51 -> 34/53 with the core profile named; read and write tool lists now carry every registered tool (gno_recall, gno_audit, the five egress read tools, gno_remember, the three egress write tools were missing). Lists verified equal to registerTools() output at HEAD (src identical to v1.45.0: git diff --stat v1.45.0 HEAD -- src docs is empty).

Counts re-derived, not copied: registerTools() driven with a fake server at HEAD gives fullRead=34, fullWrite=19, fullTotal=53, coreRead=7 (gno_context, gno_recall, gno_search, gno_query, gno_get, gno_multi_get, gno_changes), coreWrite=2 (gno_remember, gno_capture).

Copy-rule pass on the changed lines: promotional-vocabulary grep over the added lines found one evaluative phrase ("the right shape"), rewritten before commit; the remaining negative constructions are precise distinctions or honest bounds ("qmd:// URIs rather than generated answers", "authentication alone never enables them", "does not auto-embed").

Untouched by design: src/lib/product-pages.ts and src/lib/site-content.ts (task 1), src/lib/prerender-routes.ts, the memory / agents-install / protocol regions of gno-docs.tsx (tasks 1 and 2). Not pushed, not deployed; the conductor owns both.

Follow-ups outside this task's Touches (not built):
- gno.sh src/routes/integrations.index.tsx:67 still says "the fifteen mutating tools require an explicit flag" on the /integrations hub; it needs 19, and the route file could then join the STALE_TOOL_COUNTS sweep.
- gno.sh src/lib/prerender-routes.ts comparisonRouteSlugs has no "gbrain" entry (pre-existing at 20687f70): /comparisons/gbrain renders 200 via SSR and is listed on the hub, but it is absent from the prerender output and both sitemap projections.
- gno.sh integration-pages.ts header comment says "eleven different product names" while ten pages exist (comment only).

Deviation: bun run dev still 500s in this worktree (fn-133.1 cause); pages driven against the production build served with PORT=3344 node .output/server/index.mjs after the final commit's rebuild.

Staging note: the gno-repo receipt commit stages the task file by explicit path because the checkout carries pre-existing untracked .flow/artifacts/* directories that earlier workers were told not to touch.

stage: impl-review - skipped(config: REVIEW_MODE=none)
## Evidence
- Commits: e561ca64ba285fd4e15be775fb9403da19786b5d
- Tests: baseline: green via handoff (verified at 20687f70 by fn-133.2: check / typecheck / test 196 pass / build in the site worktree); the spec defines no Quick commands for the gno repo, and this task's only gno-repo change is docs/MCP.md (flowctl gate classify --base 59f72c50 -> TIER_B docs-only), site-fn-133: bun run check (rc=0 on the committed tree), site-fn-133: bun run typecheck (rc=0), site-fn-133: bun run test (rc=0; 197 passed, 5 skipped; +1 new sweep test; STALE_TOOL_COUNTS regex shown red against git show HEAD:src/lib/gno-docs.tsx and HEAD:src/lib/integration-pages.ts before the sweep, clean after), site-fn-133: bun run build (rc=0 on the committed tree; prerender lists /comparisons/qmd; sitemap carries /comparisons/qmd and /integrations/amp), driven on http://localhost:3344 (production build, PORT=3344 node .output/server/index.mjs): /comparisons/qmd 200, /comparisons/gbrain 200, /integrations/claude-desktop 200, /integrations/amp 200, /docs/mcp 200, /docs/cursor 200, /docs/claude-desktop 200, /comparisons 200; rendered-text probes: qmd page carries 'four read tools: query, get, multi_get, and status', 'AST-aware chunking', 'query-expansion model; Qmd returns ranked documents and qmd:// URIs rather than generated answers', 'GNO advertises 34 read-only tools by default and adds 19 write tools behind --enable-write, for 53 total; the opt-in core profile advertises 7 read tools (9 with writes)', 'gno_recall read-only, gno_remember behind the write flag', 'Checked against qmd 2.8.3', 'checked against the README on tobi/qmd main (commit dbfd0b4, 2026-08-18)', 'a complete tool when search is the whole job'; gbrain page carries 'gno remember for standing facts under explicit scopes ... at most 8 under 512 tokens, cited by gno:// URI', the 'Memory' bullet, 'Hermes Agent provider reads and writes it, and the OpenClaw plugin retrieves over it', 'gno_remember among them'; claude-desktop integration carries '34 read-only tools by default or a 7-tool core profile' and 'the 19 write tools, gno_remember among them, need an explicit flag'; amp carries '34 read-only retrieval, memory recall, graph, and status tools by default, or a 7-tool core profile'; docs/mcp carries 'There are 34 read-only tools by default; the opt-in core profile advertises 7 of them' and 'All 19 are disabled by default, for 53 tools total ... core profile adds only gno_capture and gno_remember'; docs/cursor carries '34 read-only tools by default' and 'Nineteen write tools are opt-in (53 total)'; docs/claude-desktop carries 'The nineteen mutating tools (create, update, move, capture, remember, reindex)'; stale scan over all eight rendered pages for 25/32/33 read-only, 15/18 write or mutating, 40/50/51 tools, 'fifteen mutating', 'Markdown only', structured_search: zero hits, truth check: MCP counts re-derived by driving registerTools() with a fake server at HEAD (src identical to v1.45.0): 34 read / 19 write / 53 total, core 7 read + 2 write; docs/MCP.md read+jobs and write lists compared equal to that output; protocol revisions 2025-11-25 and 2026-07-28 from docs/MCP.md; skill targets claude|codex|opencode|openclaw|hermes from docs/CLI.md; memory facts from docs/MEMORY.md and the site's task-1 memory copy, copy-rule sweep on the added lines: promotional-vocabulary grep -> one hit ('the right shape') rewritten before commit; negative-construction grep -> remaining hits are precise distinctions or honest bounds
- PRs: