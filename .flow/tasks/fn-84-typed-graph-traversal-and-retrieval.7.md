---
satisfies: [R7]
---

## Description

Cross-cutting documentation sweep: update every user-facing surface not already owned by a feature task, regenerate website assets, and update the hosted site. Per CLAUDE.md the spec is **not complete while hosted website docs are stale**. Depends on all feature surfaces landing (`.2`, `.5`, `.6`, `.8` — and `.3`/`.4` transitively); covers the `gno graph query`, `gno query diagnose`, and `--edge-type`/`--relation` (links/backlinks, from `.8`) surfaces.

**Size:** M (many files, low-risk prose)
**Files:** `docs/CONFIGURATION.md`, `docs/SYNTAX.md`, `docs/GLOSSARY.md`, `docs/ARCHITECTURE.md`, `docs/HOW-SEARCH-WORKS.md`, `docs/QUICKSTART.md`, `CHANGELOG.md`, `README.md` (if capabilities listed), `assets/skill/{SKILL.md,cli-reference.md,mcp-reference.md,examples.md}`, `website/_data/features.yml`, `/Users/gordon/work/gno.sh/src/lib/product-pages.ts`

## Approach

- `docs/CONFIGURATION.md`: flip `graphHints` from "reserved/no-op" to active consumption; document vocabulary meaning.
- `docs/SYNTAX.md`: add `relations:` frontmatter section (YAML shape, supported edge types, `gno://` targets, "creates typed edges at index time, no file mutation").
- `docs/GLOSSARY.md`: add `edgeType`/`relationType`, `Typed Edge`, `relations:`, `graphHints`, `Retrieval Diagnosis`.
- `docs/ARCHITECTURE.md` + `docs/HOW-SEARCH-WORKS.md`: typed-edge data model (three axes), diagnose stage-explain section.
- `docs/QUICKSTART.md`: brief `relations:` + `gno query diagnose` mentions.
- `CHANGELOG.md`: add all new surfaces under `[Unreleased] → Added`; `graphHints` now active.
- Skill assets — **canonical source is `assets/skill/`** (the npm-shipped package). Edit `assets/skill/{SKILL.md,cli-reference.md,mcp-reference.md,examples.md}` ONLY; the installed copy at `skills/gno/SKILL.md` (and any `.claude`/`.codex` siblings) is **generated by `gno skill install`** — do NOT hand-edit it; regenerate if needed. Add `gno graph query`, `gno query diagnose`, `--edge-type` to cli-reference; both MCP tools to mcp-reference + SKILL.md tool table (update tool count); add typed-graph + diagnose examples to examples.md. **Note:** per CLAUDE.md, bump ClawHub skill version only if skill text materially changed (it does here) — flag for the user, don't publish.
- Run `bun run website:sync-docs`; update `website/_data/features.yml` (typed traversal / diagnostics + MCP tool count); update hosted `gno.sh/src/lib/product-pages.ts` (graph + links feature descriptions, tool count).
- Verify with `bun scripts/docs-verify.ts` if applicable.

## Investigation targets

**Required:**

- `docs/CONFIGURATION.md` — `graphHints` row
- `docs/SYNTAX.md`, `docs/GLOSSARY.md` — sections to extend
- `assets/skill/{SKILL.md,cli-reference.md,mcp-reference.md,examples.md}` — skill content
- `website/_data/features.yml` — feature bento + tool count
- `/Users/gordon/work/gno.sh/src/lib/product-pages.ts` — hosted product copy

**Optional:**

- `docs/ARCHITECTURE.md`, `docs/HOW-SEARCH-WORKS.md`, `docs/QUICKSTART.md`, `CHANGELOG.md`
- `bun run website:sync-docs`, `bun scripts/docs-verify.ts`

## Acceptance

- [ ] All listed `docs/*.md` updated for typed edges, `relations:`, `gno graph query`, `gno query diagnose`, and active `graphHints`
- [ ] `CHANGELOG.md` `[Unreleased]` lists every new surface
- [ ] All 4 skill assets updated; ClawHub version-bump flagged to user (not auto-published)
- [ ] `website/_data/features.yml` + `gno.sh` product pages updated (incl. MCP tool count); `website:sync-docs` run
- [ ] No documentation drift: docs-verify (or manual check) passes; hosted site reflects new behavior

## Done summary
Updated cross-cutting docs, spec, bundled skill assets, repo website metadata/pages, and hosted gno.sh product copy for typed graph traversal, query diagnostics, active graphHints, relations frontmatter, semantic link filters, and 28 registered MCP tools. Ran website docs sync and verified docs/site gates. RepoPrompt review verdict SHIP.
## Evidence
- Commits:
- Tests:
- PRs: