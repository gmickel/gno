# fn146.5 final package preparation

Status: CPU preparation complete; physical CUDA/Metal execution has not started. Host full gates and an explicit GPU grant remain prerequisites. No native acceptance verdict follows from these checks.

## Immutable identities

- Product commit: `f64c41c97e196e3bffdba23bc1c006bca7489b28`, version 2.0.0.
- Published-shape npm archive: `/home/gordon/.cache/agent-tmp/gno-final-package-f64c41c9/pack/gmickel-gno-2.0.0.tgz`, SHA256 `56587f10c9969a795d6aa527c29fe8a057720a97d9f9e5de335daa996e706655`.
- Independent QA extraction: `/home/gordon/.cache/agent-tmp/fn1465-final-f64c41c9/package`. Pristine distribution untouched. All 909 shipped file hashes and the installed helper closure verified against the package owner's manifest.
- Canonical helper commit: `8b45a54d9fab684fddb69175e8c836dad4b935b2`; helper archive SHA256 `22db88f0d50558da2cb957950a56d791adae81f1865fc76841c70579f39cae04`.
- Package manifest copied to `/home/gordon/.cache/agent-tmp/fn1465-final-f64c41c9/package-receipt.json`. Existing compatible dependency install linked; no dependency installation or model download.
- Runnable configuration: `notes/fn146.5-final-run-config.json`, 709 checked product/helper/observer/fixture/driver inputs. Existing a30 configuration retained separately and not used by the final command.

## CPU checks

`notes/fn146.5-final-cpu-preflight.log`: HTTP loopback session/SSE and stdio cancellation-ID transport checks, transparent shutdown/factory forwarding, and exact dispatch/ACK observer forwarding all exited 0.

`notes/fn146.5-final-binding-preflight.json`: actual final-package writable class observers installed and restored without creating adapters, services, or native models; 27 receipts, no capture errors. Shared shutdown constants observed as 5000/5000/1000 ms, detached parent stop grace 12000 ms. `notes/fn146.5-final-shutdown-source-pins.json` records 11 source files and 23 declarations; runtime method hashes are separately captured at actual installation. The source pin generator's generic draft label does not establish physical acceptance.

Read-only existing synthetic seed SQLite `PRAGMA quick_check` returned `ok`; vector virtual-table existence does not prevent that check. Full per-table snapshot failures remain explicit rows, never silently dropped.

## CUDA command after host grant

```sh
TMPDIR=/home/gordon/.cache/agent-tmp python3 notes/fn146.5-supervise.py notes/fn146.5-final-run-config.json --native
```

The driver uses the matched29 original query and CUDA reranking policy, independently snapshots its local seed index, retains the original physical source provenance, and declares a distinct 64-document background fixture with more than 32 missing chunks. Synthetic write permission for accepted background jobs is explicit in the generated configuration; foreground/model/context options are unchanged.

Actual public REST/HTTP MCP launch uses the transparent real `startServer` background-runtime factory seam. This is explicitly an API stratum. Separate actual packaged CLI `daemon` default initial-sync and `serve` admitted native embedding SIGTERM cases remain in the same runnable plan. The task4 offline SIGINT and synthetic owned-child tests do not stand in for these physical cases.

HTTP resident shutdown and validated absence of every recorded owned native descendant must complete before stdio starts. Stdio notification and disconnect use separate owned processes, also separated by absence receipts. No simultaneous resident/stdio model allocations; no shortened model TTL. Shutdown clocks, caller cancellation, real dispatcher settlement, queue class and ACK transitions are distinct evidence channels.

## Metal staging and command

Metal requires an independently extracted final package and exact helper/observer closure staged under a new `/private/tmp/fn1465-tools-ID`, then `fn146.5-configure-metal.ts` with its final freeze descriptor. No remote staging or native run was performed during this final CPU binding step. Existing matched29 Metal setup/model paths and scratch Bun 1.3.14 remain in `notes/fn146.5-metal-preparation.md`; no database transfer across hosts.

```sh
python3 notes/fn146.5-ivan-transport.py --run /private/tmp/fn1465-tools-ID/metal-run.json --native
```

The transport reuses the owner-managed SSH master and fails closed if absent. Original Metal `noRerank:true` differs from CUDA `false` and remains explicitly pinned. Governor remains `capacity-warning30-v1`: normal pressure 1 at each isolated phase start, warning 2 cumulative at most 30 seconds, critical 4 immediate stop, 6144 MiB owned RSS cap, 120 seconds per isolated phase. Whole-driver timeout does not replace the per-phase gate. No retries, model/precision changes, or weakened thresholds.

## Remaining evidence

Physical execution and full response/model-input analysis remain outstanding on both platforms. Fast requests that escape the cancellation/overlap observation window are incomplete scenarios, not successes or automatic retry candidates. A graceful native shutdown proves its exercised branch; controlled fake-child force-exit tests remain separately labelled. Current preparation makes no stuck-kernel, fairness, native quality, or production readiness claim.
