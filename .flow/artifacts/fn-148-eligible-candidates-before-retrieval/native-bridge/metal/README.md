# Metal 201-owner filtered bridge

The matching bridge completed once on Ivan, using actual package SHA256 `1a01daff5030dfbab0492d8d7d76a759e699a3effbd87ede8a40a290e6879342` (product 9d0b57e3) and f8a278ef capture helpers. The remote overlay receipt verified 902 package files unchanged. Driver and seed hashes match the prepared `9a14793f…` and `74afced7…` versions exactly; copies live under `source/`.

The fixture was reconstructed locally from pinned synthetic text. No database was transferred. Corpus hash `06cbdc94eebdbb299c4f5525213da5fdc6671db8a5faad6198eead07bb0c9811`, all 201 document records, query and limits exactly match the CUDA manifest. All 198 active document embeddings were produced by the native model, with zero embedding errors. The same cached model bytes were used: SHA256 `06507c7b42688469c4e7298b0a1e16deff06caf291cf0a5b278c308249c3e439`. The local file URI and resulting partition identity remain distinct from Linux.

The target ranked behind 197 excluded owners in the exhaustive native vector result. At limits 1 and 10, filtered vector public rows exactly match the independent eligible selection from that complete result. Both hybrid calls return the expected sole owner. All five queries report `vectorsUsed: true`. Every owner input hash matches its current formatted text, and all 198 exact formatted strings occur in captured native inputs. The 1,024-dimensional native partition is active at its current epoch.

All 24 native requests completed, Metal only, with zero capture errors. Parent native binding loads and module lists are empty. Parent mapped-model evidence is unavailable on this platform (`mappedModels: null`), not an empty observation. The governor exited 0 after 8.204 seconds with zero accumulated warning time; its raw pressure/RSS samples remain intact. The transfer receipt reports no remaining owned native/session PIDs and normal pressure after cleanup.

The local verifier checked archive SHA256 `e00272fe468000e20e3bae76f4ffe9d6ed4281459d5c1f4fb88f714adacfe818` and all 50 transferred files before copying them into `raw/`. Files above 1 MB are compressed losslessly; `manifest.json` pins compressed bytes and original raw hashes. Database/WAL/SHM and compiled cache files were excluded by the producing worker, with the complete exclusion list retained in `transfer.json`.

This completes the requested native filtered bridge on CUDA and Metal. It does not add a full surface × corpus × platform matrix, claim cross-backend numeric equality, or independently implement hybrid score assembly. Earlier deterministic matrices supply the declared-domain full-result proof. Instrumented native duration is descriptive, not steady-state cost acceptance. Old CUDA failed attempts and all historical runner pins remain unchanged. Host owns latest integrated gates, promotion judgment and task closure.
