# Metal packed public surfaces and final warm3

Actual npm product9d0b57e3/tarballSHA1a01daff5030dfbab0492d8d7d76a759e699a3effbd87ede8a40a290e6879342. Helper98252 CLI/stdio and helperf8a HTTP retry/REST preserve original143 query/config/corpus/models. All24 exact comparison fields against final cold SDK receipt pass: results, citations, model inputs, semantic state, raw native outputs, full public results. Overall cold acceptance remains false because expansion timed out on each surface.

Final SDK cold primer plus exactly3warm in the same client: warm3 complete, all deterministic records and raw native outputs exactlyequal, resourceErrors empty, actual Metal backend. Each warm call observes models already loaded. Whole33.010691s, warning7.732574s acrossall4 calls; stricter aggregate governor, no statistical resource claim. Effective candidate/rerankdepth20; no reduced-input change. Parent binding loads/modules empty, Metal mapping coverage unavailable.

`helper98252/http` preserves the first parse-failed HTTP attempt. The exact Bun1.3.14 cached-file-length reproduction and original negative receipt remain untouched. `helperf8a278ef/http` is the separately labeled successful capture retry, still incomplete for cold expansion. Final receipts were recovered through a strict verified Heimdall→Ivan LAN route, without inference rerun or database transfer.

Retrieval manifests identify all included bytes and excluded disposable compiled/AppleDouble metadata. Prior original raw archive and final archive remain in task scratch. Existing negative evidence is never replaced.
