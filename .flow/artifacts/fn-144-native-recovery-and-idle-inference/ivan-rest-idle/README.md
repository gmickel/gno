# Ivan fn144.4 physical REST recovery

Scoped resident expiry recovery passed on committed candidate `23ba2c258e2d6c27b03aa7504a7d28f88d1ae2cf`. Eight actual REST queries returned HTTP200 and20results each. Full arrays match the historical270c initial20-result response exactly, including scores, order, full snippet bytes, source paths/mtime/hashes, conversion and egress provenance. No normalization. The fn143 comparator also passes8response-only cases; mutated-score negative rejects. This is not fn144.5 complete child-native capture.

Default TTL300000: first891.13ms, warm24.71ms, parent92937/child92944generation1. Separately TTL1200: parent92964 stays alive through child92971generation1 →93008generation2 →93047generation3. First897.92ms, warm25.14ms; after two metadata-polled expiry intervals, queries908.99ms and897.90ms, immediate repeat23.97ms. Fresh server parent93061/child93067generation1 returns identical20results in910.24ms. These are whole request/reacquisition costs; no subtraction or performance claim.

38actual `/api/status` polls during two3second idle intervals show loadedModels1→0 while the parent stays responsive. Child disappearance and successive generation argv are captured by ps ancestry. Polling does not preserve the old child. Owned-group RSS drops from approximately1.69GB to245MB after the first expiry and1.73GB to248MB after the second. All sampled Metal pressure levels remain1, no timeout/RSS/pressure watchdog stop. Owned processes verified absent after cleanup; GPU released.

The candidate built its own index locally on Ivan from the same physical30orchid source files as baseline, tokenizerunicode61, all30documents/chunks/vectors, no backlog. Query unchanged: `Who owns the meadow migration?`, collectionprobe, noExpand:true,noRerank:true. Separate cached embed/rerank/expand/gen file URIs; only embedding is exercised by this exact REST regression. Model hashes and config hashes in `raw/pins.json`, per-file corpus hashes in `raw/receipt.json`.

Source archive SHA256 `9cb8798edaae8a52a54a7c3cfd66779fcd2e074d6d5329ba7a1fbfab74fd94d3`. Bun1.3.14 scratch binary SHA256 `e0c90ec15d33363e6b70713d56bc3b2c7585c17f40a0fe0f8fd9305901d4e233`; live Bun1.3.11 untouched. Native child bootstrap records exact approved role paths/generation; status gives actual child-reported cached lifecycle counters. Metal was explicitly requested; actual child-native backend/model-input transcript was not installed, so backend intent is not mislabeled independent backend proof.

Reproduction script `run.py`; remote root `/tmp/fn1444-ivan-23ba2c25`; source archive/runtime/config/model/corpus pins and full raw requests/responses/stdout/stderr/process/status/resource data retained. Reproduction needs a NEW scratch root, exact source archive extraction and compatible existing node_modules symlink; do not overwrite this evidence. No live vault/index/service mutations or model downloads. Historical20→20→0→0→20failure remains unchanged, now alongside this repaired resident-path observation.

`fn143-response-comparison.json` explicitly marks native coverage incomplete. It projects complete unmodified public results into provenance, compares all eight against candidate initial control, and separately verifies historical initial-array equality. It neither fabricates native inputs nor implies preregistered native/performance acceptance; child-aware fn144.5 remains separate.
