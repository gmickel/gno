# CUDA packed public surfaces and matched verified Ask

Product `9d0b57e30590b5d71429df538da7a5d0f08fff02`, actual npm tarball SHA256 `1a01daff5030dfbab0492d8d7d76a759e699a3effbd87ede8a40a290e6879342`. Bun 1.3.14, node-llama-cpp 3.19.1, actual CUDA inference with existing cached pinned models. This is an extracted-package execution receipt, not a fresh dependency-install receipt. Dependencies were linked from the retained installation.

CLI and stdio MCP used frozen development helpers `98252a9c991d9451d24b77f85d3b61bcfc5bb6e0`. HTTP MCP and REST used `f8a278ef927a037c420b6c60e570fbe564ac0a13`, which fixes the Bun atomic-ledger file-length reader race. Helpers are an explicitly separate, content-pinned development overlay; they do not ship in the package. All package bytes were checked against the tarball before surface execution. Earlier successful observations were retained rather than repeated after the helper-only change.

## Public surfaces

Four real requests: CLI query, stdio MCP `gno_query`, HTTP MCP `gno_query`, REST `/api/query`. Original143 corpus, query `what retry budget did we decide and why`, limit 5, original slim-tuned models/defaults. Each surface uses a fresh process and an independent clone of the retained prequery SQLite backup; no cache clearing, priming, retries, precision changes, candidate changes, or timeout retuning. Source fixture bytes remain pinned. Seed producer was earlier d060 product; execution product is 9d0. Backup identity and timing are retained under `seed/`.

All four receipts report complete coverage and actual embedding, reranking, and expansion requests with complete inputs/outputs. Resource diagnostics and raw public responses are retained. `drivers/f8a278ef/cross-surface-comparison.json.gz` passes every full-field comparison against native_runtime's retained new-package SDK `first` result: ordered results, citations, model inputs, semantic state, all native outputs, and complete public results. The original SDK source path/phase is retained in `drivers/98252a9c/sdk-reference.json.gz`; no additional SDK inference was needed. Surface scope identities differ by definition and remain unchanged: this is explicit cross-surface equality, not a fabricated same-surface fn143 manifest comparison or latency comparison.

## Original CUDA verified Ask, matched schema 29

Baseline original product `270c3a74f4f7a3aeb8a60462b4c8e1b4adf45462` versus packed product 9d0. Original retained CUDA request: `Who owns the meadow migration?`; collection probe, limit 3, noExpand true, noRerank false, graph false. Original model/config defaults retained. The original schema27 fixture was migrated once in an isolated copy to schema29, then cloned independently for both sides. Both sides use identical relocated source paths, config, fixture, and logical index state. Actual old/new SDK preflights and the real native clients' post-open snapshots prove the same logical fingerprint `86762c89d8d365f4ce58f5287436ddef3b6d4555dbf8a46390d602bb31694e67`; config fingerprint `d65d0604e4eddb7ecafab325f75592f674da3f60ba1d6943851b42e8a9d51133`. No product source edits or compatibility shims were applied.

Both real calls completed with actual embedding, reranking, answer generation, and one closed-evidence semantic judge request each. Both returned the same abstention: “I cannot provide this answer as verified from the supplied Context Capsule.” Semantic execution completed, schema requested/enforced, candidateClaims 1, verifiedClaims 0, unresolvedClaims 1, modelCalls 1. This proves the judge actually ran; it does not claim an accepted factual answer.

`ask29/comparison.json.gz`: unchanged fn143 comparator PASS, one compared case, zero deterministic failures, no generated answer changes. All native model outputs also exactly equal. Full protocol requests/replies, model prompts/outputs, context evidence, resource receipts, actual-client logical snapshots, original manifests, preparation/preflight/comparison drivers are retained.

## Cleanup and reproduction

All six bounded native commands exited 0. No failed CUDA run was hidden or retried in this set. `drivers/f8a278ef/cleanup.json.gz` records all 16 process IDs observed in the six cases absent after completion. CUDA then released to the separate filtered-vector acceptance worker. No global process reset or unrelated process termination.

Drivers and exact paths are retained, including supervisor stdout/stderr/exit records where written. Native surface command shape: `CUDA_PATH=/opt/cuda timeout --signal=TERM --kill-after=5 150 bun --no-env-file <driver>/drive.ts --native <plan.json> <cli|stdio|http|api>`. Ask uses the same timeout wrapper and `ask29/run.ts --native <baseline|candidate>`. Existing-result guards refuse overwriting observations; reproduction requires fresh isolated roots.

`sha256.json` enumerates 301 compressed raw artifacts (5,991,246 bytes), each with original source path, decompressed payload SHA256 (`sourceSha256`), physical source-file SHA256 (`sourceFileSha256`), source encoding, retained artifact SHA256, and byte counts. JSON/text are compressed losslessly; no field normalization. Package archives, model weights, dependency trees, and SQLite databases remain machine-local and are not embedded here. Uncompressed README, checksum manifest, exclusion ledger, and curation audit are additional files. This bounded evidence does not replace the host's full CPU/package gate or the separate Ivan acceptance.

## Curation correction

The first manifest used physical source-file hashes as `sourceSha256`. Eleven sources already contained gzip bytes, so their physical hashes did not equal their decompressed payload hashes. All original 304 physical source hashes and artifact hashes verified; the eleven gzip artifacts were byte-identical to their sources. The corrected manifest consistently hashes decompressed payloads and separately preserves physical source-file identity/encoding. No retained raw artifact bytes changed.

Three generated synthetic SQLite seed artifacts (database, WAL, SHM) were inadvertently included by the initial broad scratch-file archival loop. They were moved intact to the task cache; the original scratch sources remain. `excluded-databases.json` preserves physical/payload hashes, sizes, and retained locations without database bytes. The archived `archive.py.gz` is the historical initial archival script, not a corrected curation command. `curation-audit.json` records the correction and complete revalidation. All remaining payloads were screened for SQLite content/names and common credential formats; none found. The reviewed absolute-path inventory contains only task scratch, synthetic fixture/package paths, cached model paths, repository paths, and the retained Ivan temporary package path. This bounded screening is not a general secret-detection guarantee.
