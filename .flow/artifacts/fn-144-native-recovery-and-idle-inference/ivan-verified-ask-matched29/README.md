# Matched schema29 original Metal verified Ask

Unchanged fn143 comparator passes: onecase, zero failures, zero generated-answer changes. Actual semantic judge modelCalls1 on both sides; complete coverage and equal raw native outputs. Original Metal options preserve noExpand:true/noRerank:true. The answer is a valid abstention with one unresolved claim; no full-rerank Ask claim.

Baseline product270c and candidate actual package9d0b57e3/tarball1a01 use identical schema29 SQLite backup hashes and exact same corpus/config paths. Only local same-host backups were used; no databases transferred. Prior relocated/schema27→29 failure remains separately retained in ivan-verified-ask.

`raw/comparison.json` holds the strict verdict and actual judge receipts; complete protocol replies retain full model inputs, schema/prompts, output, token/backend/model evidence. Comparator source SHA773c5dd7f284bfdcb353d7827dceae336b31fee57b7e30aea0f0ce8e9724313b remains unchanged from fn143. `compare.ts` records the executed local comparator invocation.

Both runs used warning30/6144MiB/120s governor, warning0, clean exits. Baseline10.231901s, candidate11.469605s. These are bounded correctness observations, not statistical timing claims. Metal process mappings unavailable; candidate observed parent nativebinding loads empty.
