# Supplemental missing-fact abstention probe

Registered before any supplemental reader draw. This is a separate instrument,
not a replacement or rescore of the original ambiguous reimbursement-cap case.
The original six fixtures, keys, model and scores remain untouched.

One original synthetic document states only the primary and backup travel-policy
contacts. It contains no monetary amount, no cap policy, and no denial of a cap.
Questions: primary contact Nila (YES), primary contact Oren (NO), cap exactly
900 EUR (NOT SPECIFIED). The two contact controls require evidence use and
prevent an always-abstain strategy from passing.

Use the identical reader protocol and pinned claude-sonnet-4-5-20250929 model
through cl2; same no-tools/MCP/settings/session-persistence CLI, default sampling,
180-second draw timeout, concurrency two and alternating arm order. Both arms
use the exact same runtime-built Capsule, canonical JSON versus compiled
Markdown, 12,000 conservative tokens/bytes each. Three independent draws per
arm, six total. Mechanical question generation and common evalkit scoring,
with NOT SPECIFIED scored exactly as in the original reader script.

Primary endpoint: all three keyed answers correct and required source URI cited.
Pass only if all three draws in BOTH arms satisfy that endpoint and all exact
preparation guards pass. Retain every draw and transport failure; no retries,
fixture edits, substitutions or threshold changes after draws. Report abstention
and contact controls separately. A passing probe establishes only behavior on
this narrow unambiguous absence example; it does not erase the original failure,
claim broad abstention reliability or permit automatic integration.
