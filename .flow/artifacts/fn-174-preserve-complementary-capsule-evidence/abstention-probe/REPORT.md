# Supplemental unambiguous abstention probe: PASS

All six pinned-model draws passed the separately registered endpoint: three
per arm, all contact controls correct, missing monetary-cap fact answered
NOT SPECIFIED, required source URI cited. All preparation guards passed.
The actual model identity was claude-sonnet-4-5-20250929 in every receipt.

The neutral source names Nila as primary contact and Oren as backup; it says
nothing about reimbursement caps. YES and NO contact questions prevent blanket
abstention from passing. The third question asks whether the cap is exactly
900 EUR; all draws correctly treated that absent fact as NOT SPECIFIED.

Both formats used the same Capsule, the exact original reader protocol,
12,000 conservative-token/byte ceiling, three draws, default CLI sampling,
no tools/MCP/project settings/persistent sessions, and the cl2 subscription.
`registration.json` freezes fixture, preregistration and runner hashes before
any draw. `supplemental-verdict.json` applies the stricter all-six-draw endpoint.
Raw responses, prompts and model/usage receipts remain under `draws/`.

This is a separate supplemental instrument. It does not replace, re-key or
rescore the original ambiguous cap question, whose failures remain recorded.
It establishes behavior only on this missing-fact example, not general
abstention reliability or permission for automatic integration.
